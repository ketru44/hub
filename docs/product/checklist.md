# 나만의 레시피북 MVP 티켓

이 문서는 구현 티켓, 작업 범위와 완료 판단의 기준이다. 티켓 ID를 지정해 분석, 구현 또는 상태 확인을 요청할 수 있다.

```text
FE-RECIPE-002 티켓 분석해줘
BE-AI-001 구현해줘
QA-CORE-001 상태 알려줘
```

## 티켓 규칙

- ID 형식은 `<영역>-<기능>-<3자리 번호>`를 사용하고 기존 ID를 변경하거나 재사용하지 않는다.
- 영역은 `COMMON`, `FE`, `BE`, `DB`, `QA`, `OPS`를 사용한다.
- 각 티켓은 목표, 선행 티켓, 세부 작업별 완료 기준과 티켓 검증을 포함한다.
- 상위 티켓은 모든 세부 작업과 검증이 끝났을 때만 `[x]`로 변경한다.
- 부분 구현은 증거가 있는 항목만 `[x]`로 표시하고 상위 티켓은 `[ ]`로 유지한다.
- 실제 화면, Express API, PostgreSQL과 인증·권한이 필요한 티켓은 Mock만으로 완료하지 않는다.
- 공유와 삭제·복원은 P0 핵심 흐름이 완료된 뒤 진행한다.
- P2는 선택 범위가 아니라 MVP 출시 전에 수행하는 품질·운영 단계다.
- 발표 전 추가 구현은 로그아웃, 저장된 레시피 원본 수정과 soft delete로 제한한다.
- 개인 메모와 휴지통·복원은 취소하지 않고 발표 후 구현 순서로 미루며, 열람 공유는 발표 후 가치와 필요성을 다시 검토한다.

## 티켓 적절성 기준

### 역할과 책임

- React는 화면 렌더링, 폼·UI 상태와 API 연결을 담당하고 PostgreSQL이나 Supabase Data API를 직접 호출하지 않는다.
- Express는 인증, 외부 입력 검증, 권한, 비즈니스 정책과 PostgreSQL 데이터 처리를 담당한다.
- Route는 경로와 Middleware 연결, Controller는 HTTP 입출력, Service는 비즈니스 규칙, 데이터 접근 코드는 PostgreSQL 처리를 담당한다.
- 현재 규모에서 역할이 섞이지 않는 작은 Route Handler는 계층 분리만을 위해 여러 파일로 나누지 않는다.

### 기존 구조와 최소 변경

- 새 파일이나 컴포넌트보다 기존 API Client, 인증 Middleware, 오류 응답, 화면 컴포넌트와 SQL 패턴을 우선 재사용한다.
- 현재 폴더 구조를 유지하고 실제 사용 사례가 없는 추상화, 헬퍼, 설정과 라이브러리를 추가하지 않는다.
- 티켓 완료에 필요한 파일만 수정하고 관련 없는 리팩터링을 포함하지 않는다.
- 최소 변경을 이유로 입력 검증, 오류 처리, 보안, 접근성 또는 타입 안전성을 생략하지 않는다.

## P0 완료된 기반

- [x] `COMMON-DOCS-001` 제품·화면·API·데이터 문서 초안을 정의한다.
  - 목표: MVP 구현이 참조할 제품과 기술 계약의 초안을 갖춘다.
  - 선행 티켓: 없음
  - 세부 작업:
    - [x] 제품 범위와 사용자 흐름을 문서화한다.
      - 완료 기준: PRD, 기능 명세와 사용자 흐름에서 핵심 시나리오를 확인할 수 있다.
    - [x] 화면, API와 데이터 모델을 문서화한다.
      - 완료 기준: 화면 흐름, 공통 API 계약, PostgreSQL 모델과 ERD 초안이 존재한다.
  - 검증:
    - [x] 관련 문서와 ERD 파일이 저장소에 존재한다.

- [x] `COMMON-SETUP-001` 프로젝트 기본 구조를 분리한다.
  - 목표: 프론트엔드, 백엔드와 문서 책임을 저장소 수준에서 구분한다.
  - 선행 티켓: 없음
  - 세부 작업:
    - [x] 저장소를 `frontend`, `backend`, `docs` 구조로 분리한다.
      - 완료 기준: 각 영역이 독립된 책임과 파일을 가진다.
  - 검증:
    - [x] 세 디렉터리가 저장소에 존재한다.

- [x] `FE-SETUP-001` 프론트엔드 개발 기반을 구성한다.
  - 목표: 기존 구조에서 React 화면을 구현하고 검증할 수 있게 한다.
  - 선행 티켓: `COMMON-SETUP-001`
  - 세부 작업:
    - [x] React, Vite와 Tailwind CSS 실행 환경을 구성한다.
      - 완료 기준: 개발 서버와 프로덕션 빌드가 Tailwind 스타일을 포함해 동작한다.
    - [x] React Router와 API 모듈의 기본 구조를 구성한다.
      - 완료 기준: 페이지 라우팅과 로그인·레시피 API 호출 위치가 분리되어 있다.
  - 검증:
    - [x] 프론트엔드 프로덕션 빌드가 성공한다.

- [x] `BE-SETUP-001` 백엔드 개발 기반을 구성한다.
  - 목표: Express와 TypeScript로 API를 실행할 수 있게 한다.
  - 선행 티켓: `COMMON-SETUP-001`
  - 세부 작업:
    - [x] Express와 TypeScript 실행 환경을 구성한다.
      - 완료 기준: TypeScript 소스에서 Express 애플리케이션을 실행할 수 있다.
    - [x] 서버 시작과 애플리케이션 구성을 분리한다.
      - 완료 기준: 애플리케이션 구성을 서버 시작 없이 불러올 수 있다.
  - 검증:
    - [x] 백엔드 TypeScript 빌드가 성공한다.

- [x] `FE-AUTH-001` Google 로그인 화면과 Firebase Client 기반을 작성한다.
  - 목표: 사용자가 Google 로그인을 시작하고 Firebase ID 토큰을 얻을 수 있게 한다.
  - 선행 티켓: `FE-SETUP-001`
  - 세부 작업:
    - [x] Firebase Client SDK를 환경 변수 기반으로 초기화한다.
      - 완료 기준: Firebase 비밀 credential 없이 Web App 설정만 브라우저에 제공된다.
    - [x] 반응형 로그인 화면과 Google 로그인 동작을 구현한다.
      - 완료 기준: 중복되지 않은 버튼 동작으로 `signInWithPopup`을 호출하고 ID 토큰을 얻는다.
  - 검증:
    - [x] 데스크톱과 모바일 로그인 화면 및 Google 로그인 호출을 확인한다.

- [x] `BE-AUTH-001` Firebase ID 토큰 인증 기반을 작성한다.
  - 목표: Express가 보호 API 요청의 Firebase 사용자를 신뢰할 수 있게 식별한다.
  - 선행 티켓: `BE-SETUP-001`
  - 세부 작업:
    - [x] Firebase Admin SDK를 서버 환경 변수 기반으로 초기화한다.
      - 완료 기준: Admin credential이 프론트엔드 코드나 응답에 노출되지 않는다.
    - [x] Firebase ID 토큰 검증 Middleware와 인증 경로를 작성한다.
      - 완료 기준: 유효한 Bearer 토큰만 사용자 정보를 요청에 추가하고 잘못된 요청은 401로 차단한다.
  - 검증:
    - [x] 토큰 유무에 따른 인증 응답을 확인한다.

- [x] `BE-RECIPE-001` 메모리 기반 레시피 API 프로토타입을 작성한다.
  - 목표: 데이터베이스 연결 전 레시피 API 경로와 인증 연결을 확인한다.
  - 선행 티켓: `BE-AUTH-001`
  - 세부 작업:
    - [x] 인증이 적용된 목록·상세·생성 임시 경로를 작성한다.
      - 완료 기준: 세 경로가 Express Router에서 호출되며 보호 API로 동작한다.
  - 검증:
    - [x] 임시 응답으로 세 API 경로의 연결을 확인한다.

- [x] `QA-SETUP-001` 초기 프론트엔드와 백엔드 검증을 수행한다.
  - 목표: 양쪽 프로젝트가 구현을 시작할 수 있는 빌드 상태인지 확인한다.
  - 선행 티켓: `FE-SETUP-001`, `BE-SETUP-001`
  - 세부 작업:
    - [x] 프론트엔드 프로덕션 빌드를 실행한다.
      - 완료 기준: 빌드가 오류 없이 끝난다.
    - [x] 백엔드 타입 검사를 실행한다.
      - 완료 기준: TypeScript 오류 없이 끝난다.
  - 검증:
    - [x] 실제 package script의 성공 결과를 확인한다.

- [x] `COMMON-SETUP-003` 티켓 기반 작업 운영 체계를 구축한다.
  - 목표: 고유 ID와 증거를 기준으로 티켓을 분석하고 완료할 수 있게 한다.
  - 선행 티켓: `COMMON-DOCS-001`
  - 세부 작업:
    - [x] 체크리스트, 프로젝트 컨텍스트와 작업 이력을 구성한다.
      - 완료 기준: 티켓 범위, 지속 결정과 구현 이력을 서로 구분해 기록할 수 있다.
    - [x] `run-project-ticket` 스킬과 반복 작업 후보 규칙을 구성한다.
      - 완료 기준: 티켓 요청이 선행 조건, 완료 기준과 증거를 보존하는 절차를 사용한다.
  - 검증:
    - [x] 티켓 ID, 의존성과 프로젝트 스킬 구조 검증이 성공한다.

## P0 핵심 흐름

- [x] `COMMON-DOCS-002` 충돌하는 초기 문서를 현재 정책에 맞춘다.
  - 목표: Firebase 인증과 PostgreSQL 기반 핵심 흐름의 문서 충돌을 제거한다.
  - 선행 티켓: `COMMON-DOCS-001`
  - 세부 작업:
    - [x] Google 로그인, 입력 범위와 조리 팁 제외 정책을 일치시킨다.
      - 완료 기준: 제품·화면 문서가 같은 인증과 입력 범위를 설명한다.
    - [x] Firebase UID와 PostgreSQL 모델을 ERD 문서에 일치시킨다.
      - 완료 기준: 세션 테이블 없이 사용자와 레시피 관계가 같은 이름으로 표현된다.
  - 검증:
    - [x] 제품, API, 데이터 모델과 ERD의 주요 명칭을 교차 확인한다.

- [x] `COMMON-DOCS-003` 발표 전 필수 범위와 연기 후보를 재정리한다.
  - 목표: 발표 전에는 로그아웃·원본 수정·soft delete에 집중하고 나머지 미완료 기능은 발표 후 순서로 분리한다.
  - 선행 티켓: `QA-CORE-001`, `COMMON-API-002`
  - 세부 작업:
    - [x] 발표 전 활성 티켓과 발표 후 구현 티켓을 구분한다.
      - 완료 기준: 개인 메모와 휴지통·복원이 P1에 중복되지 않고 체크리스트 마지막에 모인다.
    - [x] 활성 디자인·QA 선행 조건에서 연기 티켓을 제거한다.
      - 완료 기준: 연기 기능이 발표 전 완료 조건으로 남지 않고 제품·API 정책은 유지된다.
  - 검증:
    - [x] 티켓 ID, 선행 참조, 연기 티켓 위치와 문서 diff를 확인한다.

- [x] `COMMON-SETUP-002` 로컬 실행 방법과 환경 변수를 문서화한다.
  - 목표: 비밀값을 노출하지 않고 프론트엔드와 백엔드를 실행할 수 있게 한다.
  - 선행 티켓: `FE-SETUP-001`, `BE-SETUP-001`
  - 세부 작업:
    - [x] 실행 명령과 Firebase·API·DB 환경 변수를 README에 기록한다.
      - 완료 기준: 새 개발자가 실제 package script와 필요한 환경 변수를 찾을 수 있다.
    - [x] 비밀값 제외 규칙을 적용한다.
      - 완료 기준: `.env`와 서버 credential이 Git 추적 대상이 아니다.
  - 검증:
    - [x] README와 ignore 설정을 확인한다.

- [x] `COMMON-ASSET-001` 로그인 버튼의 누락된 가죽 질감 참조를 해결한다.
  - 목표: 존재하지 않는 에셋 때문에 로그인 화면 빌드가 경고를 내지 않게 한다.
  - 선행 티켓: `FE-AUTH-001`
  - 세부 작업:
    - [x] 누락 에셋을 추가하거나 잘못된 참조를 제거한다.
      - 완료 기준: 코드가 존재하는 에셋만 참조한다.
  - 검증:
    - [x] 프론트엔드 프로덕션 빌드에서 해당 에셋 경고가 없다.

- [x] `COMMON-API-001` 프론트엔드 API Client와 백엔드 공통 응답을 정리한다.
  - 목표: 모든 화면과 API가 같은 요청·응답·오류 처리 방식을 사용하게 한다.
  - 선행 티켓: `FE-SETUP-001`, `BE-SETUP-001`
  - 세부 작업:
    - [x] 컴포넌트의 직접 `fetch`를 공통 API 모듈로 통일한다.
      - 완료 기준: API 주소, 인증 헤더와 응답 파싱이 API Client에 모인다.
    - [x] 성공 `{ data }`와 오류 `{ error }` 계약을 적용한다.
      - 완료 기준: 일반 오류, 잘못된 응답과 네트워크 오류를 안정적인 코드로 구분한다.
  - 검증:
    - [x] 공통 404·500과 프론트엔드 오류 변환을 확인한다.

- [x] `BE-SETUP-002` Express 공통 보안 설정을 확정한다.
  - 목표: 모든 API에 필요한 최소 전송·입력·오류 보안을 적용한다.
  - 선행 티켓: `BE-SETUP-001`
  - 세부 작업:
    - [x] 환경별 CORS Origin과 JSON 본문 크기 제한을 적용한다.
      - 완료 기준: 허용 Origin만 교차 출처 응답을 받고 과도한 본문은 거부된다.
    - [x] 운영 오류에서 내부 정보를 숨긴다.
      - 완료 기준: Stack Trace와 credential이 API 응답에 포함되지 않는다.
  - 검증:
    - [x] 허용·비허용 Origin, 큰 요청과 내부 오류 응답을 확인한다.

- [x] `DB-CORE-001` PostgreSQL 핵심 데이터 접근 기반을 구축한다.
  - 목표: 서비스 사용자와 핵심 레시피 데이터를 PostgreSQL에 안전하게 저장한다.
  - 선행 티켓: `COMMON-DOCS-002`
  - 세부 작업:
    - [x] `pg` Pool, 환경 변수 검증과 순차 SQL 마이그레이션을 구성한다.
      - 완료 기준: `DATABASE_URL`로 연결하고 마이그레이션을 반복 가능한 명령으로 실행한다.
    - [x] 핵심 테이블, 제약, 인덱스와 삭제 정책을 생성한다.
      - 완료 기준: 사용자·레시피·재료·단계·출처가 문서의 무결성 규칙을 만족한다.
    - [x] 매개변수화 SQL과 트랜잭션 기반 데이터 접근을 사용한다.
      - 완료 기준: 사용자 입력을 SQL 문자열에 직접 결합하지 않는다.
  - 검증:
    - [x] 마이그레이션, 핵심 제약과 백엔드 타입 검사를 확인한다.

- [x] `BE-AUTH-002` Firebase 사용자를 서비스 사용자와 연결한다.
  - 목표: 인증된 Firebase UID를 PostgreSQL 내부 사용자로 연결한다.
  - 선행 티켓: `BE-AUTH-001`, `DB-CORE-001`, `COMMON-API-001`
  - 세부 작업:
    - [x] Firebase UID로 사용자를 조회하거나 생성한다.
      - 완료 기준: 같은 UID는 같은 내부 사용자 ID를 반환하고 이메일은 식별 키로 사용하지 않는다.
    - [x] `/api/auth/me`가 `CurrentUser` 계약을 반환한다.
      - 완료 기준: 인증 credential 없이 내부 사용자 정보만 `{ data }`로 반환한다.
  - 검증:
    - [x] 첫 로그인과 재로그인, 무토큰 요청을 실제 DB로 확인한다.

- [x] `FE-AUTH-002` Google 로그인 성공·실패 흐름을 완성한다.
  - 목표: 사용자가 중복 요청 없이 로그인하고 실패 후 재시도할 수 있게 한다.
  - 선행 티켓: `FE-AUTH-001`, `BE-AUTH-002`, `COMMON-API-001`
  - 세부 작업:
    - [x] 처리 중 상태, 실패 메시지와 재시도를 구현한다.
      - 완료 기준: 처리 중 중복 클릭이 차단되고 실패 후 버튼이 다시 활성화된다.
    - [x] API 모듈로 현재 사용자를 확인하고 목록으로 이동한다.
      - 완료 기준: 유효한 Firebase ID 토큰으로 `/api/auth/me` 성공 후 `/recipes`로 이동한다.
  - 검증:
    - [x] 로그인 성공·실패와 프론트엔드 lint·build를 확인한다.

- [x] `FE-AUTH-003` 인증 확인과 보호 라우트를 구현한다.
  - 목표: 인증 상태가 확정되기 전에 보호 화면을 노출하지 않는다.
  - 선행 티켓: `FE-AUTH-002`
  - 세부 작업:
    - [x] Firebase 인증 확인 중 상태를 제공한다.
      - 완료 기준: 인증 확인 전 로그인 또는 보호 화면이 잘못 표시되지 않는다.
    - [x] 비로그인 접근과 로그인 후 원래 경로 복귀를 구현한다.
      - 완료 기준: 보호 경로는 로그인으로 이동하고 전달 링크는 로그인 후 복원된다.
  - 검증:
    - [x] 일반 보호 경로와 전달 링크 복귀를 확인한다.

- [x] `BE-RECIPE-002` 사용자별 레시피 목록 API를 구현한다.
  - 목표: 현재 사용자의 활성 `OWNED`, `EXTERNAL` 레시피 목록을 PostgreSQL에서 조회한다.
  - 선행 티켓: `DB-CORE-001`, `BE-AUTH-002`
  - 세부 작업:
    - [x] Firebase UID로 내부 사용자를 식별하고 활성 레시피만 최신순으로 조회한다.
      - 완료 기준: 다른 사용자와 soft delete 레시피가 응답에 포함되지 않는다.
    - [x] 응답을 `RecipeSummary` 계약에 맞춘다.
      - 완료 기준: 유형과 출처 정보가 `{ data: RecipeSummary[] }`로 반환된다.
  - 검증:
    - [x] 무토큰 401, 사용자 분리, 최신순, 빈 목록과 백엔드 type-check·build를 확인한다.
  - 후속: `RECEIVED`와 관계 정보는 `BE-SHARE-003`에서 목록·상세에 통합한다.

- [x] `FE-RECIPE-001` 내 레시피북 목록 화면을 구현한다.
  - 목표: 로그인 사용자가 자신의 레시피 목록과 주요 상태를 확인한다.
  - 선행 티켓: `FE-AUTH-003`, `BE-RECIPE-002`
  - 세부 작업:
    - [x] 기존 API 모듈로 목록을 조회하고 필터를 제공한다.
      - 완료 기준: 전체·내가 등록한 레시피·전달받은 레시피 필터가 서버 데이터를 기준으로 동작한다.
    - [x] 카드와 비동기 상태를 구현한다.
      - 완료 기준: 제목, 설명, 유형, 출처 또는 관계 정보와 로딩·빈 상태·오류가 표시된다.
  - 검증:
    - [x] 실제 목록 API와 프론트엔드 lint·build를 확인한다.
  - 후속: 추가 진입은 `FE-RECIPE-002`, 상세 이동은 `FE-RECIPE-004`에서 연결한다.

- [x] `FE-RECIPE-002` 레시피 추가 입력 화면을 구현한다.
  - 목표: 사용자가 URL, 직접 입력 또는 두 입력을 함께 작성하고 AI 정리를 요청할 준비를 한다.
  - 선행 티켓: `FE-RECIPE-001`
  - 세부 작업:
    - [x] 기존 책형 레이아웃 안에 추가 페이지와 라우트를 구현한다.
      - 완료 기준: 목록의 추가 행동으로 접근하고 뒤로 이동할 수 있다.
    - [x] URL 입력과 직접 입력 폼을 구현한다.
      - 완료 기준: Label과 도움말이 있고 두 값 중 하나 이상을 요구하며 오류는 입력 근처에 표시된다.
    - [x] 폼 값을 다음 AI 요청 단계로 전달한다.
      - 완료 기준: 두 입력을 함께 유지하며 전달 초대 코드 진입점과 섞이지 않는다.
    - [x] 모바일·키보드 접근성을 적용한다.
      - 완료 기준: 390px 너비에서 깨지지 않고 Label, Focus와 버튼 의미가 명확하다.
  - 검증:
    - [x] 빈 입력, URL만, 직접 입력만, 혼합 입력과 프론트엔드 lint·build를 확인한다.

- [x] `COMMON-AI-001` AI 제공자와 운영 기준을 확정한다.
  - 목표: 주요 기술을 임의 도입하지 않고 MVP AI 연결 결정을 기록한다.
  - 선행 티켓: `COMMON-DOCS-002`
  - 세부 작업:
    - [x] 사용자 승인을 받아 AI 제공자와 모델을 선택한다.
      - 완료 기준: 비용, 구조화 출력 지원, 공식 문서와 3주 MVP 적합성을 근거로 결정한다.
    - [x] 환경 변수, 요청 제한과 실패 정책을 문서화한다.
      - 완료 기준: 비밀 키는 백엔드에만 두고 Timeout과 사용자 오류 방식을 명시한다.
  - 검증:
    - [x] 결정된 기술이 AGENTS.md와 README에 반영되고 임의 의존성이 추가되지 않았는지 확인한다.

- [x] `BE-AI-001` 직접 입력 AI 레시피 구조화 API를 구현한다.
  - 목표: 인증된 사용자의 직접 입력을 검증된 편집용 초안으로 변환한다.
  - 선행 티켓: `BE-AUTH-002`, `COMMON-API-001`, `COMMON-AI-001`
  - 세부 작업:
    - [x] `POST /api/ai/recipes/structure`의 직접 입력 경로를 구현한다.
      - 완료 기준: 빈 입력과 본문 길이 초과를 4xx로 거부하고 검증된 입력만 Service로 전달한다.
    - [x] 선택한 AI 제공자를 서버에서 호출한다.
      - 완료 기준: API 키가 노출되지 않고 Timeout과 제공자 실패가 안정적인 오류 코드로 변환된다.
    - [x] AI 응답을 `RecipeDraft`, `RecipeWarning`으로 검증한다.
      - 완료 기준: 잘못된 응답은 저장되지 않고 `AI_RESPONSE_INVALID`로 처리된다.
    - [x] 초안만 반환하고 DB에 저장하지 않는다.
      - 완료 기준: 성공 응답 이후 레시피 테이블에 새 행이 생기지 않는다.
  - 검증:
    - [x] 정상 입력, 빈 입력, 길이 제한, AI 실패·잘못된 응답과 백엔드 type-check·build를 확인한다.

- [x] `BE-AI-002` 안전한 URL 수집과 혼합 입력을 구현한다.
  - 목표: 외부 URL에서 안전하게 텍스트를 수집하고 직접 입력과 함께 AI 구조화에 사용한다.
  - 선행 티켓: `BE-AI-001`
  - 세부 작업:
    - [x] `http`, `https` URL과 DNS 결과를 검증한다.
      - 완료 기준: localhost, 사설·링크 로컬·예약 IP와 허용하지 않는 프로토콜이 차단된다.
    - [x] Redirect마다 목적지를 재검증하고 수집 한계를 적용한다.
      - 완료 기준: Timeout, Redirect 횟수, 응답 크기와 본문 길이 제한을 넘으면 중단된다.
    - [x] HTML에서 레시피 관련 텍스트만 추출한다.
      - 완료 기준: HTML 전체나 실행 가능한 콘텐츠를 AI에 그대로 전달하지 않는다.
    - [x] URL 단독과 URL·직접 입력 혼합 요청을 구조화한다.
      - 완료 기준: 직접 입력은 보완 정보로 사용되고 수집 실패는 직접 입력을 안내하는 오류로 반환된다.
  - 검증:
    - [x] 공개 URL, Redirect, 차단 주소, 과대·지연 응답, URL 단독과 혼합 입력을 확인한다.

- [x] `COMMON-AI-002` 공개 YouTube 영상의 자막 수집 정책과 기술을 확정한다.
  - 목표: 공개 YouTube 영상 자막을 임의로 자동 수집하지 않는 MVP 지원 범위와 실패 방식을 사용자 승인으로 확정한다.
  - 선행 티켓: `COMMON-AI-001`, `BE-AI-002`
  - 세부 작업:
    - [x] 지원 영상과 실패 경계를 확정한다.
      - 완료 기준: 임의의 공개 제3자 YouTube 영상은 자막 자동 수집 지원 범위에서 제외하고, YouTube URL은 기존 `URL_FETCH_FAILED` 422와 직접 입력 안내로 처리한다.
    - [x] 자막과 영상 메타데이터를 가져올 기술을 선택한다.
      - 완료 기준: 공식 YouTube Data API 메타데이터만으로는 레시피 원문을 얻을 수 없고 자막은 OAuth 및 영상 편집자 권한이 필요함을 확인한다. 비공식 스크래핑·내부 엔드포인트는 채택하지 않는다.
    - [x] 보안·운영 기준과 관련 문서를 갱신한다.
      - 완료 기준: 새 의존성, OAuth, API 키, 환경 변수나 오류 코드를 추가하지 않고 승인된 지원 제외 및 기존 실패 계약을 관련 문서에 일관되게 반영한다.
  - 검증:
    - [x] 승인 전 임의 의존성이 추가되지 않았고 지원 범위와 실패 정책이 관련 문서에서 일치하는지 확인한다.

- [x] `BE-AI-003` 공개 YouTube 영상의 자막 기반 레시피 구조화는 MVP 범위에서 제외한다.
  - 목표: `COMMON-AI-002`의 승인된 정책에 따라 임의 공개 YouTube 영상의 자막 수집 구현을 진행하지 않는다.
  - 선행 티켓: `COMMON-AI-002`, `BE-AI-002`
  - 세부 작업:
    - [x] 비공식 스크래핑·내부 엔드포인트, OAuth, 자막 수집 의존성을 추가하지 않는다.
      - 완료 기준: 기존 일반 URL 수집 경로를 유지하며 YouTube URL 수집 실패는 현재 `URL_FETCH_FAILED` 422와 직접 입력 안내로 반환한다.
  - 검증:
    - [x] 코드·의존성·환경 변수 변경 없이 관련 문서의 지원 제외와 기존 실패 계약이 일치함을 확인한다.

- [x] `BE-AI-005` 공개 YouTube 영상의 자막 우선 레시피 구조화를 구현한다.
  - 목표: 공개 YouTube URL의 제목·채널명과 공개 자막 또는 Gemini 영상 분석 결과를 기존 OpenAI 구조화·검증 흐름에 연결한다.
  - 선행 티켓: `BE-AI-002`, `COMMON-AI-001`
  - 세부 작업:
    - [x] YouTube URL과 video ID를 검증하고 공식 메타데이터를 조회한다.
      - 완료 기준: 공개 `youtube.com`, `youtu.be` URL만 전용 경로로 분기하며, YouTube Data API의 영상 제목·채널명을 출처의 `title`, `author`로 사용한다. 썸네일은 조회·저장·표시하지 않는다.
    - [x] 공개 또는 자동 생성 자막을 우선 수집한다.
      - 완료 기준: 고정된 `youtube-transcript-api` Python 의존성으로 자막을 한 번만 조회하고, 프록시·쿠키·계정 인증·차단 우회·자막 저장을 사용하지 않는다. 검증된 video ID만 shell 없이 Python 프로세스에 전달하고 10초 timeout과 20,000자 출력 길이 제한을 적용한다.
    - [x] 자막 실패 시 Gemini 영상 분석으로 대체한다.
      - 완료 기준: `GEMINI_MODEL=gemini-3.6-flash`에는 원본 URL·제목·채널명만 전달하고 30초 timeout·무재시도를 적용한다. 자막 또는 Gemini 결과는 기존 OpenAI `RecipeDraft` 구조화·검증 단계로 전달하며 API 키·영상·자막 원문·Gemini 응답 전문은 로그에 남기지 않는다.
    - [x] YouTube 실패 계약과 직접 입력 대체를 유지한다.
      - 완료 기준: 메타데이터, 자막과 Gemini 경로가 모두 실패하면 기존 `URL_FETCH_FAILED` 422와 직접 입력 안내를 반환하고 URL·직접 입력 혼합 요청의 보완 정보는 유지한다.
  - 검증:
    - [x] YouTube Data API, Python 자막 프로세스, Gemini와 OpenAI를 모킹해 자막 성공·자막 없음·차단·timeout·Gemini 실패와 출처 매핑을 검증한다.
    - [x] 기존 일반 URL 수집 회귀와 백엔드 test·type-check·build, 프론트엔드 test·lint·build를 확인한다.

- [x] `COMMON-AI-003` 공개 네이버 블로그 게시물을 현재 MVP/P0 입력 범위에서 제외한다.
  - 목표: 사용자 승인에 따라 공개 네이버 블로그 URL을 현재 MVP/P0의 성공 입력 범위에서 제외하고 직접 입력 대체 흐름을 확정한다.
  - 상태: 범위 제외 결정으로 종료했으며 네이버 블로그 수집 기술 확정이나 구현 완료를 의미하지 않는다.
  - 선행 티켓: `COMMON-AI-001`, `BE-AI-002`
  - 세부 작업:
    - [x] 지원 범위와 실패 경계를 확정한다.
      - 완료 기준: 일반 공개 웹페이지와 공개 YouTube URL은 기존 지원 범위로 유지하고 `blog.naver.com` 계열 URL은 `URL_FETCH_FAILED` 422와 직접 입력 안내로 처리한다.
    - [x] 네이버 블로그 전용 수집 기술을 도입하지 않는다.
      - 완료 기준: 공식 검색 API, 브라우저 렌더링, 전용 스크래핑과 새 의존성을 현재 MVP/P0에 추가하지 않는다.
    - [x] 보안·운영 기준과 관련 문서를 갱신한다.
      - 완료 기준: 네이버 블로그 URL을 외부 요청 전에 차단하고 기존 요청 제한, 로그 제외와 오류 계약을 변경하지 않은 채 관련 제품·API 문서에 같은 지원 범위를 반영한다.
  - 검증:
    - [x] 전용 의존성 추가 없이 네이버 블로그 URL의 실패 경계를 Red→Green 테스트로 확인하고 지원 범위와 직접 입력 대체 흐름이 관련 문서에서 일치하는지 확인한다.

- [x] `BE-AI-004` 공개 네이버 블로그 전용 구조화를 현재 MVP/P0 범위에서 제외한다.
  - 목표: 네이버 블로그 전용 수집을 구현하지 않고 해당 URL을 기존 URL 수집 실패와 직접 입력 대체 흐름으로 연결한다.
  - 상태: 범위 제외로 종료했으며 네이버 블로그 전용 구조화 구현 완료를 의미하지 않는다.
  - 선행 티켓: `COMMON-AI-003`, `BE-AI-002`
  - 세부 작업:
    - [x] 네이버 블로그 전용 수집과 의존성을 추가하지 않는다.
      - 완료 기준: 일반 공개 웹페이지와 공개 YouTube의 기존 수집 경로를 유지하고 네이버 전용 API·브라우저 렌더링·스크래핑을 추가하지 않는다.
    - [x] 네이버 블로그 URL을 직접 입력 대체 흐름으로 연결한다.
      - 완료 기준: `blog.naver.com`과 하위 도메인을 외부 요청 전에 `URL_FETCH_FAILED`로 거부하고 기존 422 응답과 직접 입력 안내를 사용한다.
  - 검증:
    - [x] 네이버 블로그 호스트와 하위 도메인 거부 테스트, 백엔드 test·type-check·build를 통과한다.

- [x] `FE-AI-001` AI 구조화 요청과 상태 흐름을 연결한다.
  - 목표: 세 입력 방식으로 실제 AI API를 호출하고 초안 편집 단계로 이동한다.
  - 선행 티켓: `FE-RECIPE-002`, `BE-AI-002`
  - 세부 작업:
    - [x] 기존 API Client에 AI 요청 함수를 추가한다.
      - 완료 기준: 컴포넌트에 API 주소나 직접 `fetch`가 없고 Firebase ID 토큰이 전달된다.
    - [x] 처리 중, 성공과 실패 상태를 구현한다.
      - 완료 기준: 중복 제출을 막고 실패 후 입력값을 유지하며 재시도할 수 있다.
    - [x] 검증된 초안을 편집 화면으로 전달한다.
      - 완료 기준: 새로고침이나 직접 URL 접근 시 임의의 빈 초안을 저장 단계로 넘기지 않는다.
  - 검증:
    - [x] 세 입력 방식, 인증 실패, URL 수집 실패와 AI 실패를 실제 API로 확인한다.

- [x] `FE-AI-002` AI 초안 확인·수정 화면을 구현한다.
  - 목표: 사용자가 AI 결과를 검토하고 수정한 뒤 저장 여부를 결정한다.
  - 선행 티켓: `FE-AI-001`
  - 세부 작업:
    - [x] 제목, 설명, 인원과 조리 시간을 편집한다.
      - 완료 기준: 필수값과 숫자 범위 오류가 해당 입력 근처에 표시된다.
    - [x] 재료와 조리 단계를 편집한다.
      - 완료 기준: 이름·수량·단위·내용과 순서를 수정하고 빈 항목을 검증한다.
    - [x] 확인 필요 항목과 출처를 표시한다.
      - 완료 기준: 경고가 관련 입력과 연결되고 출처는 본문보다 낮은 정보 계층을 사용한다.
    - [x] 초안 손실 정책과 접근성을 적용한다.
      - 완료 기준: 취소·이탈 동작이 일관되고 Label, Focus와 모바일 레이아웃이 동작한다.
  - 검증:
    - [x] 모든 편집 필드, 배열 오류, 경고 연결, 이탈과 프론트엔드 lint·build를 확인한다.

- [x] `BE-RECIPE-003` 수정된 레시피 초안을 PostgreSQL에 저장한다.
  - 목표: 인증 사용자가 확인한 초안을 하나의 트랜잭션으로 저장한다.
  - 선행 티켓: `DB-CORE-001`, `COMMON-API-001`
  - 세부 작업:
    - [x] `POST /api/recipes`를 `CreateRecipeRequest` 계약에 맞춘다.
      - 완료 기준: 서버가 입력 전체를 다시 검증하고 로그인 사용자로 `ownerId`를 설정한다.
    - [x] 레시피 유형과 출처를 서버에서 결정한다.
      - 완료 기준: 출처가 있으면 `EXTERNAL`, 없으면 `OWNED`이며 일반 API로 `RECEIVED`를 만들 수 없다.
    - [x] 레시피와 하위 데이터를 트랜잭션으로 저장한다.
      - 완료 기준: 재료·단계 순서를 검증하고 한 단계 실패 시 모든 저장이 롤백된다.
  - 검증:
    - [x] 두 유형의 정상 저장, 잘못된 입력, 위조 owner·type과 트랜잭션 롤백을 확인한다.

- [x] `FE-RECIPE-003` 레시피 저장 UX를 구현한다.
  - 목표: 편집한 초안을 실제 저장 API로 보내고 실패 시 작업을 잃지 않는다.
  - 선행 티켓: `FE-AI-002`, `BE-RECIPE-003`
  - 세부 작업:
    - [x] 기존 API 모듈로 저장 요청을 연결한다.
      - 완료 기준: 중복 저장을 차단하고 서버 검증 오류를 편집 화면에 표시한다.
    - [x] 성공과 실패 후 상태를 처리한다.
      - 완료 기준: 실패 후 편집값이 유지되고 성공 시 생성된 레시피 ID를 보존한다.
  - 검증:
    - [x] 정상 저장, 중복 클릭, 검증 실패, 네트워크 실패와 입력 유지를 확인한다.

- [x] `BE-RECIPE-004` 소유권이 적용된 상세 조회 API를 구현한다.
  - 목표: 로그인 사용자가 소유한 활성 레시피 상세만 조회한다.
  - 선행 티켓: `BE-RECIPE-003`
  - 세부 작업:
    - [x] 레시피와 재료·단계·출처를 PostgreSQL에서 조회한다.
      - 완료 기준: `RecipeDetail` 계약과 저장 순서를 유지한다.
    - [x] 소유권과 활성 상태를 서버에서 검사한다.
      - 완료 기준: 다른 사용자와 삭제된 레시피는 정보 노출 없이 `RECIPE_NOT_FOUND`를 반환한다.
  - 검증:
    - [x] 정상 상세, 없는 ID, 타인 소유와 삭제 레시피를 확인한다.

- [x] `FE-RECIPE-004` 레시피 상세와 이동 흐름을 구현한다.
  - 목표: 저장된 레시피를 읽기 중심으로 보고 목록과 저장 흐름에서 이동한다.
  - 선행 티켓: `FE-RECIPE-003`, `BE-RECIPE-004`
  - 세부 작업:
    - [x] 상세 페이지와 API 연결을 구현한다.
      - 완료 기준: 제목, 설명, 인분, 시간, 재료, 단계와 출처를 서버 응답으로 표시한다.
    - [x] 목록 카드와 저장 성공 이동을 상세 경로에 연결한다.
      - 완료 기준: 두 진입 방식이 같은 상세 화면을 열고 목록으로 복귀할 수 있다.
    - [x] 로딩·오류·반응형·접근성을 구현한다.
      - 완료 기준: 모바일과 태블릿에서 읽을 수 있고 키보드 Focus와 오류 상태가 명확하다.
  - 검증:
    - [x] 저장→상세, 목록→상세, 오류→목록 복귀와 프론트엔드 lint·build를 확인한다.

- [x] `QA-CORE-001` 핵심 레시피 흐름을 통합 검증한다.
  - 목표: 로그인부터 저장된 레시피 재확인까지 실제 API와 DB로 완주한다.
  - 선행 티켓: `FE-RECIPE-004`, `BE-AI-005`
  - 세부 작업:
    - [x] URL, 직접 입력과 혼합 입력 흐름을 각각 수행한다.
      - 완료 기준: 직접 입력, 지원 가능한 일반 공개 웹페이지 URL과 공개 YouTube URL 입력 흐름이 편집 가능한 AI 초안을 반환한다. YouTube의 전체 처리 실패는 `URL_FETCH_FAILED` 422와 직접 입력 안내를 확인한다.
    - [x] 초안을 수정하고 PostgreSQL에 저장한다.
      - 완료 기준: 저장 결과가 상세와 목록 재조회에서 동일하게 보인다.
    - [x] 핵심 실패 흐름을 수행한다.
      - 완료 기준: 인증, 입력, URL 수집, AI와 저장 실패가 입력 손실이나 권한 노출 없이 처리되며, 현재 범위에서 제외한 네이버 블로그 URL은 `URL_FETCH_FAILED` 422와 직접 입력 안내로 처리된다.
  - 검증:
    - [x] 실제 Google 로그인, Express API와 PostgreSQL을 사용한 전체 시나리오를 기록한다.

## P1 MVP 기능 완성

- [x] `FE-AUTH-004` 로그아웃을 구현한다.
  - 목표: 사용자가 현재 Firebase 인증 상태를 안전하게 종료하고 로그인 화면으로 돌아간다.
  - 선행 티켓: `FE-AUTH-003`
  - 세부 작업:
    - [x] Firebase Client SDK로 로그아웃하고 로그인 화면으로 이동한다.
      - 완료 기준: 별도 백엔드 로그아웃 API 없이 `signOut` 후 보호 화면에 남지 않는다.
    - [x] 처리 중과 실패 상태를 구현한다.
      - 완료 기준: 중복 실행을 막고 실패 시 현재 화면을 유지하며 다시 시도할 수 있다.
  - 검증:
    - [x] 로그아웃 성공·실패, 보호 경로 차단과 재로그인을 확인한다.

- [x] `FE-RECIPE-005` 조리 중 보기를 구현한다.
  - 목표: 상세 화면에서 관리 동작을 숨기고 레시피 읽기에 집중한다.
  - 선행 티켓: `QA-CORE-001`
  - 세부 작업:
    - [x] 상세 화면에 조리 중 보기 토글을 추가한다.
      - 완료 기준: 관리 버튼만 숨고 레시피 본문과 메모는 유지된다.
    - [x] 일반 상세 복귀를 제공한다.
      - 완료 기준: 새 데이터나 별도 조리 상태를 저장하지 않고 즉시 복귀한다.
  - 검증:
    - [x] 모바일에서 토글 전후 내용과 키보드 접근을 확인한다.

- [x] `COMMON-API-002` 수정·삭제·복원 API 계약을 확정한다.
  - 목표: 구현 전에 누락된 후속 레시피 API 계약과 권한을 결정한다.
  - 선행 티켓: `QA-CORE-001`
  - 세부 작업:
    - [x] 원본 수정 API의 요청·응답과 오류를 정의한다.
      - 완료 기준: `OWNED`, `EXTERNAL` 편집 필드와 `RECEIVED` 차단 방식이 명확하다.
    - [x] 삭제·휴지통·복원 API 계약을 정의한다.
      - 완료 기준: soft delete, 30일 보관, 응답 형태와 route 순서가 명확하다.
  - 검증:
    - [x] API 명세, 기능 정책과 데이터 모델이 같은 권한·필드를 사용하는지 확인한다.

- [x] `BE-RECIPE-005` 레시피 원본 수정 API를 구현한다.
  - 목표: 소유자가 `OWNED`, `EXTERNAL` 레시피 내용을 수정한다.
  - 선행 티켓: `COMMON-API-002`, `BE-RECIPE-004`
  - 세부 작업:
    - [x] 확정된 수정 계약과 서버 검증을 구현한다.
      - 완료 기준: owner·type은 클라이언트 값으로 바뀌지 않고 `RECEIVED`는 차단된다.
    - [x] 하위 데이터를 트랜잭션으로 갱신한다.
      - 완료 기준: 재료·단계 실패 시 기존 레시피 전체가 유지된다.
  - 검증:
    - [x] 두 수정 가능 유형, `RECEIVED`, 타인 소유와 롤백을 확인한다.

- [x] `FE-RECIPE-006` 레시피 원본 수정 화면을 구현한다.
  - 목표: 수정 가능한 레시피를 편집하고 상세로 돌아간다.
  - 선행 티켓: `BE-RECIPE-005`, `FE-AI-002`
  - 세부 작업:
    - [x] 기존 초안 편집 UI를 재사용해 수정 화면을 구현한다.
      - 완료 기준: 중복 폼을 만들지 않고 서버 상세를 편집 상태로 변환한다.
    - [x] 저장·실패·취소 흐름을 구현한다.
      - 완료 기준: 실패 후 값이 유지되고 `RECEIVED`에는 원본 수정 진입점이 없다.
  - 검증:
    - [x] 두 수정 가능 유형, 실패와 취소를 확인한다.

- [x] `DB-AUDIT-001` 레시피 감사 이벤트 데이터 구조를 구축한다.
  - 목표: 삭제·복원과 공유의 보안 관련 행위를 최소 정보로 기록한다.
  - 선행 티켓: `QA-CORE-001`, `DB-CORE-001`
  - 세부 작업:
    - [x] `recipe_audit_events` 마이그레이션과 인덱스를 작성한다.
      - 완료 기준: 사용자, 레시피, 행위와 시각을 조회할 수 있다.
    - [x] 민감정보 제외 규칙을 적용한다.
      - 완료 기준: 토큰, credential과 레시피 본문을 저장할 컬럼이 없다.
  - 검증:
    - [x] 마이그레이션 적용·재실행과 데이터 모델 일치를 확인한다.

- [x] `DB-SHARE-002` 전달 초대와 관계 데이터 구조를 구축한다.
  - 목표: 일회성 전달 초대와 받은 레시피 관계 정보를 저장한다.
  - 선행 티켓: `QA-CORE-001`, `DB-AUDIT-001`
  - 세부 작업:
    - [x] `transfer_invitations`, `received_recipe_details` 마이그레이션을 작성한다.
      - 완료 기준: 스냅샷, 만료·사용 상태와 관계 정보가 데이터 모델을 따른다.
    - [x] 토큰·코드와 수락 조회 인덱스를 작성한다.
      - 완료 기준: 원문 없이 해시로 찾고 동시 수락을 제약할 수 있다.
  - 검증:
    - [x] 마이그레이션, 외래 키, 만료와 단일 사용 제약을 확인한다.

- [x] `BE-RECIPE-007` 레시피 soft delete API를 구현한다.
  - 목표: 저장 레시피를 유형 정책에 따라 레시피북에서 제거한다.
  - 선행 티켓: `COMMON-API-002`, `DB-AUDIT-001`, `DB-SHARE-002`
  - 세부 작업:
    - [x] `DELETE /api/recipes/:recipeId`를 구현한다.
      - 완료 기준: 소유권을 검사하고 `deletedAt`을 기록하며 일반 목록·상세에서 제외한다.
    - [x] 유형별 삭제 의미와 감사 기록을 적용한다.
      - 완료 기준: `OWNED`·`EXTERNAL`은 삭제, `RECEIVED`는 내 레시피북 제거로 기록된다.
  - 검증:
    - [x] 세 유형, 타인 소유, 중복 삭제와 감사 이벤트를 확인한다.

- [x] `FE-RECIPE-008` 레시피 삭제·제거 UX를 구현한다.
  - 목표: 유형에 맞는 문구로 저장 레시피를 삭제하거나 내 레시피북에서 제거한다.
  - 선행 티켓: `BE-RECIPE-007`
  - 세부 작업:
    - [x] 유형별 확인과 처리 상태를 구현한다.
      - 완료 기준: `RECEIVED`에는 원본 삭제가 아닌 내 레시피북 제거로 표시된다.
    - [x] 삭제 성공과 실패 흐름을 구현한다.
      - 완료 기준: 중복 요청을 막고 성공 후 목록으로 이동하며 실패 시 상세 화면과 오류 안내를 유지한다.
  - 검증:
    - [x] 세 유형의 삭제·제거 성공, 실패, 타인 소유와 확인 취소를 확인한다.

- [x] `BE-SHARE-002` 전달 초대 생성과 미리보기 API를 구현한다.
  - 목표: `OWNED` 레시피의 일회성 링크·코드와 공통 미리보기를 제공한다.
  - 선행 티켓: `DB-SHARE-002`
  - 세부 작업:
    - [x] 초대 생성과 스냅샷 저장을 구현한다.
      - 완료 기준: 링크 토큰과 코드는 한 번만 반환되고 각각 안전한 해시로 저장되며 7일 후 만료된다.
    - [x] 링크·코드 미리보기 API를 구현한다.
      - 완료 기준: 두 진입 방식이 같은 계약과 만료·사용 오류를 반환한다.
    - [x] 민감값 로그 제외를 적용한다.
      - 완료 기준: 토큰과 코드 원문이 Query, 애플리케이션 로그와 오류에 남지 않는다.
  - 검증:
    - [x] 유형·소유권, 두 진입 방식, 만료·사용 상태와 로그를 확인한다.

- [x] `BE-SHARE-003` 전달 초대 수락과 받은 레시피 통합을 구현한다.
  - 목표: 한 사용자만 초대를 수락하고 관계 정보가 포함된 `RECEIVED` 레시피를 저장한다.
  - 선행 티켓: `BE-SHARE-002`, `BE-RECIPE-004`
  - 세부 작업:
    - [x] 관계 입력과 자기 초대 여부를 검증한다.
      - 완료 기준: 전해준 사람·관계 라벨은 필수이고 자기 초대는 거부된다.
    - [x] 수락과 저장을 하나의 트랜잭션으로 처리한다.
      - 완료 기준: 동시 요청 중 하나만 성공하고 실패 시 초대와 레시피가 함께 롤백된다.
    - [x] 목록·상세에 `RECEIVED` 관계 정보를 통합한다.
      - 완료 기준: 받은 날짜와 관계 정보가 소유자에게 보이며 원본 수정과 모든 재공유가 차단된다.
  - 검증:
    - [x] 정상·중복·동시·자기 수락, 롤백과 목록·상세 재조회를 확인한다.

- [x] `FE-SHARE-002` 전달 코드·링크 미리보기 화면을 구현한다.
  - 목표: 링크 또는 별도 코드 입력으로 저장 전 레시피를 확인한다.
  - 선행 티켓: `BE-SHARE-002`, `FE-AUTH-003`
  - 세부 작업:
    - [x] 일반 추가 폼과 분리된 코드 입력을 구현한다.
      - 완료 기준: 빈·잘못된·만료·사용 코드를 입력 근처 오류로 구분한다.
    - [x] 링크·코드 공통 미리보기 화면을 구현한다.
      - 완료 기준: 비로그인은 로그인 후 같은 초대로 복귀해 미리보기를 보고 수락할 수 있다.
    - [x] 수락·거절 초기 상태를 구현한다.
      - 완료 기준: 거절은 서버 상태를 변경하지 않고 화면을 종료한다.
  - 검증:
    - [x] 두 진입 방식, 로그인 복귀와 초대 상태 오류를 확인한다.

- [x] `FE-SHARE-003` 전달 수락·관계 입력·저장 흐름을 구현한다.
  - 목표: 사용자가 관계 정보를 입력하고 받은 레시피를 자신의 레시피북에 저장한다.
  - 선행 티켓: `BE-SHARE-003`, `FE-SHARE-002`
  - 세부 작업:
    - [x] 수락 후 관계 정보와 선택 메모 폼을 표시한다.
      - 완료 기준: 필수 오류가 입력 근처에 표시되고 취소 시 저장되지 않는다.
    - [x] 저장 상태와 받은 레시피 이동을 구현한다.
      - 완료 기준: 중복 수락을 막고 성공 후 관계 정보가 있는 상세와 목록으로 이어진다.
  - 검증:
    - [x] 정상 수락, 중복·동시 오류, 실패 후 입력 유지와 상세 이동을 확인한다.

- [x] `FE-SHARE-004` 전달 초대 생성과 공유 UI를 구현한다.
  - 목표: 소유자가 `OWNED` 레시피의 일회성 전달 링크와 초대 코드를 생성해 다른 사용자에게 전달한다.
  - 선행 티켓: `BE-SHARE-002`, `FE-RECIPE-004`, `FE-SHARE-003`
  - 세부 작업:
    - [x] 레시피 상세에 유형별 전달 초대 생성 동작을 연결한다.
      - 완료 기준: `OWNED`에만 생성 진입점을 제공하고 인증된 생성 요청의 처리 중 중복 제출과 권한·서버 오류를 처리한다.
    - [x] 생성된 전달 링크와 초대 코드를 공유할 수 있게 표시한다.
      - 완료 기준: 현재 프론트엔드 Origin의 전체 링크와 코드를 선택·복사할 수 있고 복사 성공·실패를 안내하며 생성 결과를 유지한다.
    - [x] 전달 초대의 사용 조건을 안내한다.
      - 완료 기준: 링크와 코드는 같은 초대이며 한 명의 수락 후 사용할 수 없고 7일 후 만료되며, 원문은 생성 응답에서만 확인할 수 있음을 표시한다.
  - 검증:
    - [x] `OWNED` 생성과 `EXTERNAL`·`RECEIVED` 차단, 중복 생성 방지, 복사 상태를 확인하고 다른 Google 계정에서 코드·링크 미리보기와 수락·상세 이동까지 확인한다.

## P2 품질 및 운영

- [x] `FE-ARCH-001` 책형 레시피 화면의 컴포넌트와 라우트 구조를 리팩터링한다.
  - 목표: 여러 화면 책임이 결합된 `RecipeListPlaceholderPage`를 책형 Layout과 독립 route page로 분리하고, 기존 기능과 사용자 경험을 유지한다.
  - 선행 티켓: `FE-AUTH-004`, `FE-RECIPE-005`, `FE-RECIPE-006`, `FE-RECIPE-008`, `FE-SHARE-003`, `FE-SHARE-004`
  - 세부 작업:
    - [x] 보호된 책형 Layout과 중첩 라우트로 목록·추가·상세·전달 코드 화면을 분리한다.
      - 완료 기준: `/recipes`, `/recipes/new`, `/recipes/:recipeId`, `/transfer-invitations`가 공통 책형 Layout 아래에서 기존과 동일하게 동작한다.
    - [x] 레이아웃·내비게이션·목록·route page의 상태와 렌더링 책임을 분리한다.
      - 완료 기준: 목록 조회와 갱신은 Layout, 상세 조회·삭제·조리 모드는 상세 page, AI 구조화는 추가 page가 담당하며 pathname 기반 콘텐츠 분기를 제거한다.
    - [x] 반복되는 폼 액션에 제한된 공통 Button을 적용하고 테스트를 책임별로 재구성한다.
      - 완료 기준: Primary·Secondary·Danger 액션만 재사용하고 링크·필터·메뉴·아이콘 버튼은 범용화하지 않으며 기존 반응형·Focus·오류·중복 요청 방지 동작을 유지한다.
  - 검증:
    - [x] 목록·추가·상세·전달 코드의 성공·오류·빈 상태와 모바일 메뉴·Dialog Focus를 확인하고 프론트엔드 전체 test·lint·build를 통과한다.
  - 후속: `FE-DESIGN-001`

- [ ] `FE-DESIGN-001` 핵심 화면의 디자인 일관성을 감사한다.
  - 목표: 기능별 접근성을 다시 구현하지 않고 전체 화면의 디자인 규칙 편차를 정리한다.
  - 선행 티켓: `FE-AUTH-004`, `FE-RECIPE-005`, `FE-RECIPE-006`, `FE-RECIPE-008`, `FE-SHARE-003`, `FE-SHARE-004`, `FE-ARCH-001`
  - 세부 작업:
    - [ ] 기존 디자인 토큰과 책형 패턴의 사용 편차를 수정한다.
      - 완료 기준: 색상·타이포·간격·Radius와 Primary Action이 디자인 문서를 따른다.
    - [ ] 전체 반응형과 접근성을 교차 점검한다.
      - 완료 기준: 390px, 태블릿, 낮은 높이, Safe Area, 키보드, Focus, 대비와 reduced motion을 만족한다.
  - 검증:
    - [ ] 핵심 화면 전체를 지정 viewport와 키보드로 확인하고 lint·build를 실행한다.

- [ ] `COMMON-QA-001` 최소 자동 테스트 전략과 도구를 확정한다.
  - 목표: 미확정 테스트 기술을 사용자 승인으로 선택하고 유지 가능한 범위를 정한다.
  - 선행 티켓: `QA-CORE-001`
  - 세부 작업:
    - [ ] 현재 코드와 package script를 기준으로 테스트 도구를 비교한다.
      - 완료 기준: 프론트엔드, 백엔드와 E2E 중 회귀 위험이 큰 최소 범위를 제안한다.
    - [ ] 사용자 승인 후 도구와 실행 명령을 문서화한다.
      - 완료 기준: 새 의존성의 목적과 로컬 실행 방법이 README에 기록된다.
  - 검증:
    - [ ] 최소 예제 테스트가 선택한 명령으로 성공한다.

- [ ] `QA-AUTH-001` 인증 흐름 자동 테스트를 작성한다.
  - 목표: 인증 우회와 로그인 상태 회귀를 자동으로 탐지한다.
  - 선행 티켓: `COMMON-QA-001`, `FE-AUTH-003`, `BE-AUTH-002`
  - 세부 작업:
    - [ ] 정상·누락·만료·위조 토큰을 테스트한다.
      - 완료 기준: 보호 API의 성공과 401 응답이 자동 검증된다.
    - [ ] 로그인 처리 중·실패·성공 화면을 테스트한다.
      - 완료 기준: 중복 요청, 오류 노출과 경로 복귀가 자동 검증된다.
  - 검증:
    - [ ] 인증 테스트 명령이 반복 실행해 성공한다.

- [ ] `QA-RECIPE-001` 레시피와 AI 핵심 자동 테스트를 작성한다.
  - 목표: 입력, 권한, AI, 저장 트랜잭션과 화면 상태 회귀를 자동 탐지한다.
  - 선행 티켓: `COMMON-QA-001`, `QA-CORE-001`
  - 세부 작업:
    - [ ] 입력 검증, URL 보안과 AI 응답 검증을 테스트한다.
      - 완료 기준: 차단 입력과 외부 실패가 안정적인 오류 코드로 검증된다.
    - [ ] 소유권, 저장 트랜잭션과 실패 롤백을 테스트한다.
      - 완료 기준: 타인 접근과 부분 저장이 발생하지 않는다.
    - [ ] 로딩·빈 상태·오류와 입력 유지를 테스트한다.
      - 완료 기준: 핵심 프론트엔드 실패 상태가 자동 검증된다.
  - 검증:
    - [ ] 레시피 테스트 명령이 격리된 데이터로 반복 성공한다.

- [ ] `QA-SHARE-001` 공유 정책과 동시성 자동 테스트를 작성한다.
  - 목표: 전달 공유 권한과 일회성 수락 정책의 회귀를 탐지한다.
  - 선행 티켓: `COMMON-QA-001`, `FE-SHARE-003`, `FE-SHARE-004`
  - 세부 작업:
    - [ ] 유형별 전달 공유 권한과 스냅샷 경계를 테스트한다.
      - 완료 기준: `OWNED`만 생성할 수 있고 초대 스냅샷에 개인 메모·내부 사용자가 없다.
    - [ ] 초대 만료·중복·동시 수락을 테스트한다.
      - 완료 기준: 한 초대로 하나의 받은 레시피만 생성된다.
    - [ ] 토큰과 코드 로그 마스킹을 확인한다.
      - 완료 기준: 원문이 수집 가능한 로그에 남지 않는다.
  - 검증:
    - [ ] 공유 테스트 명령이 반복 실행해 성공한다.

- [ ] `QA-SECURITY-001` 비밀값과 개인정보 노출을 점검한다.
  - 목표: 인증·레시피·공유 경계에서 민감정보가 노출되지 않게 한다.
  - 선행 티켓: `QA-AUTH-001`, `QA-RECIPE-001`, `QA-SHARE-001`
  - 세부 작업:
    - [ ] Git 추적 파일과 브라우저 번들을 점검한다.
      - 완료 기준: DB, Firebase Admin, AI와 공유 비밀값이 포함되지 않는다.
    - [ ] 로그와 오류 응답을 점검한다.
      - 완료 기준: ID 토큰, 초대 원문, 개인정보와 내부 Stack Trace가 없다.
  - 검증:
    - [ ] 저장소·빌드 산출물 검색과 대표 운영 오류 요청 결과를 기록한다.

- [ ] `OPS-DEPLOY-001` 운영 배포 환경을 구성한다.
  - 목표: 사용자 승인을 받은 서비스에 프론트엔드, Express와 PostgreSQL을 안전하게 배포한다.
  - 선행 티켓: `QA-SECURITY-001`
  - 세부 작업:
    - [ ] 배포 서비스를 비교하고 사용자 승인을 받는다.
      - 완료 기준: 비용, 유지보수, React·Express·PostgreSQL 연결을 근거로 결정한다.
    - [ ] 운영 환경 변수, CORS와 PostgreSQL 연결을 구성한다.
      - 완료 기준: 비밀값이 서버에만 있고 프론트엔드 Origin과 백엔드 URL이 정확히 연결된다.
    - [ ] 실행·마이그레이션·배포 방법을 문서화한다.
      - 완료 기준: 새 환경에서 같은 순서로 배포를 재현할 수 있다.
  - 검증:
    - [ ] 운영 health, 프론트엔드 접근, 인증 API와 마이그레이션 상태를 확인한다.

- [ ] `QA-RELEASE-001` 운영 환경에서 발표 범위를 검증한다.
  - 목표: 운영 환경의 핵심 흐름과 발표 전 활성 기능이 시연 가능한지 판정한다.
  - 선행 티켓: `OPS-DEPLOY-001`, `FE-DESIGN-001`, `QA-AUTH-001`, `QA-RECIPE-001`, `QA-SHARE-001`
  - 세부 작업:
    - [ ] 저장소의 모든 실제 검증 명령을 실행한다.
      - 완료 기준: 프론트엔드 lint·build, 백엔드 type-check·build와 자동 테스트가 성공한다.
    - [ ] 핵심 레시피 흐름을 운영 API와 DB로 수행한다.
      - 완료 기준: 로그인부터 저장·상세·목록 재확인까지 성공한다.
    - [ ] 로그아웃, 원본 수정·soft delete와 전달 공유를 수행한다.
      - 완료 기준: 발표 전 활성 기능의 유형별 권한과 정상·오류 흐름이 문서 정책과 일치한다.
  - 검증:
    - [ ] 운영 체크 결과, 발견 문제와 발표 준비 판정을 기록한다.

## P3 발표 후 연기 후보

이 절의 기능은 취소하거나 제품 정책에서 제거한 것이 아니다. 개인 메모와 휴지통·복원은 발표 후 프로젝트 완성 단계에서 구현하고, 열람 공유는 가치와 필요성을 다시 검토한 뒤 구현 여부를 결정한다. 연기 기능을 구현할 때 관련 디자인 감사와 최종 출시 검증 범위에도 다시 포함한다.

### 발표 후 구현

- [ ] `BE-RECIPE-006` 개인 메모 API를 구현한다.
  - 목표: 소유자가 모든 저장 레시피에 개인 메모를 작성한다.
  - 선행 티켓: `BE-RECIPE-004`
  - 세부 작업:
    - [ ] `PATCH /api/recipes/:recipeId/memo`를 구현한다.
      - 완료 기준: 서버가 소유권과 길이를 검증하고 빈 메모를 `null`로 저장한다.
    - [ ] 상세 응답에 소유자의 메모만 포함한다.
      - 완료 기준: 공개·타인 응답과 AI 입력에 개인 메모가 포함되지 않는다.
  - 검증:
    - [ ] 작성·수정·삭제, 타인 접근과 길이 오류를 확인한다.

- [ ] `FE-RECIPE-007` 개인 메모 화면을 구현한다.
  - 목표: 상세 화면에서 개인 메모를 잃지 않고 저장한다.
  - 선행 티켓: `BE-RECIPE-006`, `FE-RECIPE-004`
  - 세부 작업:
    - [ ] 메모 입력과 저장 상태를 구현한다.
      - 완료 기준: 처리 중 중복 요청을 막고 실패 후 입력값을 유지한다.
    - [ ] 유형과 접근 상태에 맞게 메모를 노출한다.
      - 완료 기준: 소유한 저장 레시피에만 편집 UI가 표시된다.
  - 검증:
    - [ ] 저장 성공·실패·재조회와 모바일 입력을 확인한다.

- [ ] `BE-RECIPE-008` 휴지통 조회와 복원 API를 구현한다.
  - 목표: 사용자가 30일 이내 삭제 레시피를 조회하고 복원한다.
  - 선행 티켓: `BE-RECIPE-007`
  - 세부 작업:
    - [ ] 휴지통 목록과 route 순서를 구현한다.
      - 완료 기준: `GET /api/recipes/trash`가 recipeId 상세보다 먼저 매칭되고 소유 데이터만 반환한다.
    - [ ] 복원 API와 감사 기록을 구현한다.
      - 완료 기준: 보관 기간 내 레시피만 복원되고 일반 목록에 다시 나타난다.
  - 검증:
    - [ ] 빈 휴지통, 정상 복원, 타인 소유와 기간 경과를 확인한다.

- [ ] `FE-RECIPE-009` 휴지통과 복원 화면을 구현한다.
  - 목표: 삭제 직후 실행 취소를 제공하고 휴지통에서 보관 기간과 복원을 관리한다.
  - 선행 티켓: `BE-RECIPE-008`, `FE-RECIPE-008`
  - 세부 작업:
    - [ ] 삭제 직후 복원 행동과 휴지통 목록 상태를 구현한다.
      - 완료 기준: 실행 취소가 복원 API로 이어지고 삭제 시각과 남은 기간이 표시된다.
    - [ ] 복원 상태와 목록 복귀를 구현한다.
      - 완료 기준: 중복 요청을 막고 복원 후 일반 목록에서 확인할 수 있다.
  - 검증:
    - [ ] 실행 취소, 휴지통 조회, 복원 성공·실패와 반응형 화면을 확인한다.

### 가치 재검토 후 결정

- [ ] `DB-SHARE-001` 열람 공유 데이터 구조를 구축한다.
  - 목표: 활성 상태를 가진 열람 링크를 PostgreSQL에 저장한다.
  - 선행 티켓: `QA-CORE-001`, `DB-AUDIT-001`
  - 세부 작업:
    - [ ] `recipe_view_shares` 마이그레이션과 인덱스를 작성한다.
      - 완료 기준: 해시 토큰, 활성 상태, 원본 레시피와 생성 시각을 관리한다.
  - 검증:
    - [ ] 고유 토큰, 원본 관계와 활성 링크 조회를 확인한다.

- [ ] `BE-SHARE-001` 열람 공유 API를 구현한다.
  - 목표: 공유 가능한 레시피의 비로그인 읽기 전용 링크를 관리한다.
  - 선행 티켓: `DB-SHARE-001`
  - 세부 작업:
    - [ ] 링크 생성·재생성·비활성화 API를 구현한다.
      - 완료 기준: `OWNED`, `EXTERNAL` 소유자만 실행하고 재생성 시 이전 링크가 비활성화된다.
    - [ ] 비로그인 열람 API를 구현한다.
      - 완료 기준: 개인 메모·사용자 내부 정보가 없고 비활성·삭제 원본은 조회되지 않는다.
  - 검증:
    - [ ] 유형·소유권, 링크 수명과 공개 응답 개인정보를 확인한다.

- [ ] `FE-SHARE-001` 열람 공유 생성과 공개 열람 화면을 구현한다.
  - 목표: 소유자가 링크를 관리하고 방문자는 로그인 없이 읽기만 한다.
  - 선행 티켓: `BE-SHARE-001`
  - 세부 작업:
    - [ ] 링크 생성·복사·비활성화 UI를 구현한다.
      - 완료 기준: 유형별 허용 동작만 보이고 처리 상태가 표시된다.
    - [ ] 공개 열람 전용 페이지를 구현한다.
      - 완료 기준: 저장·수정·메모·재공유 진입점이 없고 비활성 오류가 표시된다.
  - 검증:
    - [ ] 로그인·비로그인과 활성·비활성 링크를 확인한다.
